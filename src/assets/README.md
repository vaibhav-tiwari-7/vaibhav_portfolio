This folder is kept for parity with the requested structure, but static files that
are referenced by URL (profile photo, resume PDF, project images) should actually
live in `/public/assets/` — that's what Vite serves at runtime as `/assets/...`.

See `/public/assets/README.md` for the exact filenames to drop in.
